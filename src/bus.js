// It's probably not worth using a state management library (vuex).
// Opting for a global event bus for now.
// For usage see http://vuejs.org/guide/components.html#Non-Parent-Child-Communication

import Vue from 'vue'

export default new Vue();
